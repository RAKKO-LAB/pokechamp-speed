# ポケモンチャンピオンズ 素早さ早見表

## 概要
ポケモンチャンピオンズ（ポケチャン）の素早さ比較ツール。HTML単体ファイルで完結し、GitHub Pagesで公開する。

## 技術構成
- **HTML + React (CDN)**: `index.html` 1ファイルで完結
- **Babel standalone**: JSX変換用（CDN読み込み）
- **localStorage**: マイボックス・マイパーティのデータ永続化
- **ホスティング**: GitHub Pages（mainブランチ / rootから配信）

## ファイル構成
```
pokechamp-speed/
├── CLAUDE.md      # このファイル
└── index.html     # アプリ本体（これだけでOK）
```

## 主要機能

### 素早さ早見表（メインリスト）
- 全269匹（通常ポケモン + メガシンカ）のS実数値をライン別にグループ表示
- 同じ実数値のポケモンが同じ行にまとまる
- ひらがな入力で即座にカタカナ変換検索（「か」→カバルドン等）
- タイプでも検索可能

### 性格・振り方（複数同時選択）
- 最速 / 準速 / 無振 / 最遅を複数ON可能
- 選択した条件分だけ同じポケモンが別エントリで表示される
- 各エントリにタグ表示（【最速】【準速】等）

### 補正・特性
- 🧣 スカーフ（全ポケモンに×1.5ライン追加）
- 💨 追い風（×2）
- ⚡ まひ（×0.5）
- ✦ 特性倍速（該当ポケモンのみ自動表示）
  - 【軽】かるわざ ×2: オオニューラ、ルチャブル、レパルダス、ペロリーム
  - 【葉】ようりょくそ ×2: フシギバナ、ウツボット、リーフィア、エルフーン、スコヴィラン
  - 【砂】すなかき ×2: ドリュウズ、ルガルガン(まひる)
  - 【雪】ゆきかき ×2: ツンベアー
  - 【水】すいすい ×2: イダイトウ♂♀、ツンベアー
  - 【波】サーフテール ×2: アローラライチュウ
  - 【加1】かそく ×1.5 / 【加2】×2.0: クエスパトラ
  - 【ス】スカーフ持ちデフォルト ×1.5: イダイトウ♂
- メガシンカ表示/非表示トグル

### マイパーティ
- ポケモンを検索→性格・能力P（0〜32）設定→実数値自動計算で登録
- こだわりスカーフトグルあり（計算モード時×1.5）
- 直接実数値入力モードも選択可能
- ニックネーム設定可能（「スカーフガブ」等の型メモ）
- 登録数の上限なし（メガ前後の両方登録可能）
- 右サイドバーに固定表示（スクロールしても常時見える）
  - ニックネーム or ポケモン名を2行折り返しで表示
  - メガ → 「メガ / フーディン」のように改行
  - スカーフ持ちは【ス】表示
- パーティから外すと📦アイコンでボックスに戻す

### マイボックス（localStorage永続化）
- 登録したポケモンはボックスに蓄積される
- ボックスからパーティに出し入れ自由
- ブラウザを閉じてもデータ保持
- ボックス内で✎編集、×削除が可能
- 「+ ボックスに追加」ボタンでボックスのみに登録（パーティには入れない）

### ポケモン選択 & フィルタ
- メインリストのチップをタップで選択/解除（◆オレンジで表示）
- 「選択のみ表示」ボタンで、選択+パーティのポケモンだけに絞り込み
- 活用例: 見せ合い中に相手の6匹を選択 → 自分vs相手だけの早見表で対戦

## ポケモンデータの編集
`index.html` 内の `const RAW = [...]` 配列がデータ本体。

### フォーマット
```javascript
{n:"ポケモン名", b:素早さ種族値, t:"タイプ1/タイプ2"}           // 通常
{n:"メガ○○", b:素早さ種族値, t:"タイプ", m:1}                  // メガシンカ
{n:"ポケモン名", b:素早さ種族値, t:"タイプ", ab:["特性キー"]}   // 特性持ち
```

### 特性キー一覧
- `karuwaza` - かるわざ（×2）
- `suisui` - すいすい（×2）
- `sunakaki` - すなかき（×2）
- `youryoku` - ようりょくそ（×2）
- `yukikaki` - ゆきかき（×2）
- `surftail` - サーフテール（×2）
- `kasoku` - かそく（×1.5 + ×2.0の2段階）
- `scarf_default` - スカーフ持ちデフォルト（×1.5）

### ポケモン追加例
```javascript
{n:"新ポケモン", b:100, t:"ほのお/ドラゴン"},
{n:"メガ新ポケモン", b:120, t:"ほのお/ドラゴン", m:1},
{n:"特性持ち", b:88, t:"じめん/はがね", ab:["sunakaki"]},
```

## 素早さ実数値の計算式
チャンピオンズ仕様: 個体値31固定、Lv50
```
実数値 = floor( ( floor( (種族値×2 + 31 + 能力P×2) × 50/100 ) + 5 ) × 性格補正 )
```
- 能力P: 0〜32（本家の努力値÷8に相当）
- 性格補正: ×1.1（上昇）/ ×1.0（無補正）/ ×0.9（下降）

## 現在チャンピオンズに未登場のポケモン（RAWから除外済み）
以下はチャンピオンズの内定ポケモンリスト（pokemon_champions_roster_current.csv）に含まれていなかったため除外したポケモン。今後のアップデートで追加される可能性あり。
パオジアン、ハバタクカミ、テツノツツミ、イーユイ、ディンルー、ナットレイ、ボーマンダ、キノガッサ、コノヨザル、パーモット、ハピナス、ラッキー、ポリゴン2、ヘイラッシャ、メガライチュウX/Y

## GitHub Pages デプロイ
```bash
git add .
git commit -m "update"
git push origin main
```
pushすれば数分で反映される。

## 今後の拡張候補
- 相手パーティの画像認識（カメラ→ポケモン自動特定）
- AdSense広告の追加
- DLC追加ポケモンへの対応
- レギュレーション変更への追従
